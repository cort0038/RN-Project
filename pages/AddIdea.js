import {
	SafeAreaView,
	ScrollView,
	Text,
	Image,
	Button,
	StyleSheet,
	View
} from "react-native"

import {styles} from "../utils/styles"

export const AddIdea = ({navigation}) => {
	return (
		<SafeAreaView>
			<ScrollView>
				<Text>Add Idea Page</Text>
			</ScrollView>
		</SafeAreaView>
	)
}
