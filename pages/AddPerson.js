import {
	SafeAreaView,
	ScrollView,
	Text,
	Image,
	Button,
	StyleSheet,
	View
} from "react-native"

import {styles} from "../utils/styles"

export const AddPerson = ({navigation}) => {
	return (
		<SafeAreaView>
			<ScrollView>
				<Text>Add Person Page</Text>
			</ScrollView>
		</SafeAreaView>
	)
}
