import {
	SafeAreaView,
	ScrollView,
	Text,
	Image,
	Button,
	StyleSheet,
	View
} from "react-native"

import {styles} from "../utils/styles"

export const Idea = ({navigation}) => {
	return (
		<SafeAreaView>
			<ScrollView>
				<Text>Idea Page</Text>
			</ScrollView>
		</SafeAreaView>
	)
}
