import {
	SafeAreaView,
	ScrollView,
	Text,
	Image,
	Button,
	StyleSheet,
	View
} from "react-native"

import { styles } from "../utils/styles"

export const People = ({ navigation }) => {
    return (
        <SafeAreaView>
            <ScrollView>
                <Text>People Page</Text>
            </ScrollView>
        </SafeAreaView>
    )
}
